$ rails new example -d postgresql
$ cd example
$ git add . && git commit # ready to deploy?
$ firefox https://provisioning.sh # fill form, copy link
$ export DIGITALOCEAN_TOKEN=xxxxxxx
$ curl -sSL "https://provisioning.sh?uuid=xxxxxxx" | bash
$ git push dokku master # deployed :)
